/**
 * config/index.ts
 * Reads and validates all environment variables at startup using Zod.
 * If any required variable is missing the process exits immediately with a
 * clear error — no silent failures.
 *
 * Implementation: next session.
 */
export {};
