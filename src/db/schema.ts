/**
 * db/schema.ts
 * Drizzle ORM table definitions for the Nott AI database.
 *
 * Tables:
 *   clients   — one row per registered business
 *
 * Keep this file minimal. Only add columns we actually use.
 *
 * Implementation: next session.
 */

// Placeholder — will be implemented with drizzle-orm/better-sqlite3
export {};
