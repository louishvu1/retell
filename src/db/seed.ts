/**
 * db/seed.ts
 * Development utility — inserts the first test client into the database.
 *
 * Run with: npx ts-node src/db/seed.ts
 *
 * You will need to fill in your Retell agent ID before running.
 * The Square tokens will be populated later when the business owner
 * completes the OAuth flow at /oauth/square/start.
 *
 * Implementation: next session.
 */
export {};
